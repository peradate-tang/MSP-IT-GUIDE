# Changelog

ทุกการแก้ไขนับจากนี้จะขึ้นเวอร์ชันตาม [Semantic Versioning](https://semver.org/) —
`MAJOR.MINOR.PATCH` เช่น `1.2.0`

- **MAJOR** (x.0.0) — เปลี่ยนแปลงที่กระทบโครงสร้างสิทธิ์/ข้อมูลเดิม ต้อง migrate
- **MINOR** (1.x.0) — เพิ่มฟีเจอร์ใหม่ ไม่กระทบของเดิม
- **PATCH** (1.0.x) — แก้บั๊ก/ปรับเล็กน้อย

เลขเวอร์ชันจะซิงก์กันทั้ง `backend/package.json` และ `frontend/package.json` เสมอ
และแสดงผลในแอปจริงที่: มุมล่างซ้ายของ sidebar, หน้า login, และ endpoint `GET /api/health`

---

## [1.2.1] — 2026-08-04

### เปลี่ยนแปลง
- **เปลี่ยนชื่อระบบจาก "IT Guide" เป็น "Staff Guide"** ทุกจุดในแอป (sidebar, หน้า login, browser title, หัวกระดาษตอนพิมพ์, รายงาน) เพราะตอนนี้ระบบใช้แชร์ความรู้ของทุกแผนกในโรงแรม ไม่ได้จำกัดแค่ทีม IT แล้ว
- แก้ข้อความหน้าแรก (hero) ที่เดิมพูดถึง "ทีม IT" โดยเฉพาะ ให้เป็นข้อความกลางๆ ครอบคลุมทุกแผนก
- อัปเดตชื่อใน README, package.json description, และค่า default ฝั่ง backend ให้สอดคล้องกัน (หมวดหมู่ "IT" ในฐานะแผนกหนึ่งยังคงอยู่เหมือนเดิม ไม่ได้ถูกลบ)

## [1.2.0] — 2026-08-04

### แก้ไข (บั๊ก)
- แก้บั๊ก **"เห็นเมนู Admin แต่กดเข้าไม่ได้"** — สาเหตุคือ sidebar เช็คสิทธิ์ไม่ตรงกับ route guard (sidebar เช็ครวม permission `*` แต่หน้าเว็บจริงเช็คแค่ role name `admin` เป๊ะๆ) ตอนนี้ใช้ logic ชุดเดียวกันทั้งสองจุดผ่าน `frontend/src/lib/permissions.ts`

### เพิ่ม
- เพิ่มสิทธิ์ **"เข้าหน้า Admin Panel"** (`admin:access`) เป็นติ๊กเลือกแยกต่างหากในหน้า จัดการ Role — ให้สิทธิ์เข้าหน้า Admin Panel (users/roles/categories/report) กับ role ไหนก็ได้ โดยไม่ต้องให้สิทธิ์ `*` (ซึ่งจะข้ามข้อจำกัดแผนกไปด้วย) ถ้าไม่ติ๊กไว้ จะทั้งมองไม่เห็นเมนูและเข้าหน้านั้นไม่ได้เลย
- เพิ่มคำเตือนอธิบายชัดเจนใต้ติ๊ก "* — สิทธิ์ทั้งหมด" ว่ามันครอบคลุมสิทธิ์ admin panel ไปด้วย กันความสับสนที่ทำให้เกิดบั๊กนี้ตั้งแต่แรก

## [1.1.0] — 2026-08-04

### เพิ่ม
- ระบบ **จัดการเวอร์ชัน** — เลขเวอร์ชันแสดงในแอป (sidebar, login, `/api/health`) และเริ่มเก็บ changelog ไฟล์นี้
- ปุ่ม **พิมพ์บทความ** พร้อม layout เฉพาะตอนพิมพ์ (หัวกระดาษ/ท้ายกระดาษ, โค้ดบล็อกพิมพ์ง่าย, ลิงก์แสดง URL, ซ่อนเมนู/ปุ่มที่ไม่เกี่ยว)
- ปรับ **โมเดลสิทธิ์ใหม่**: ผูก "แผนก" ของแต่ละ user เข้ากับหมวดหมู่ (Category) โดยตรง — editor สร้าง/แก้ไข/ลบบทความได้เฉพาะหมวดของแผนกตัวเอง, อ่านบทความไม่ต้อง login, ตัด role "viewer" ที่ไม่จำเป็นแล้วออก
- หน้า "บทความของแผนกฉัน" ให้ editor จัดการบทความของตัวเองได้โดยไม่ต้องเป็น admin
- ปรับ **UI/UX ใหม่ทั้งระบบ** จากธีมมืดสไตล์ developer tool → โทนอบอุ่นแบบ hospitality (กระดาษไอวอรี่ + ทองเหลือง + ตัวอักษรเซอริฟ) เหมาะกับพนักงานโรงแรมทุกแผนก
- เพิ่ม endpoint `GET /api/health` สำหรับเช็คสถานะ service/DB (ใช้กับ Railway health check ได้)
- เพิ่มฟีเจอร์ค้นหาบทความครอบคลุม title + content + tags (เดิมค้นได้แค่ title)
- เพิ่ม DTO validation (`class-validator`) ทุก endpoint, rate limiting กัน brute-force login, บังคับตั้ง `JWT_SECRET` ก่อน deploy จริง
- ลบไฟล์ Cloudinary ที่ไม่ได้ใช้แล้วอัตโนมัติเมื่อแก้ไขบทความ (กันไฟล์กำพร้า)

### แก้ไข (บั๊ก/ความปลอดภัย)
- เพิ่ม endpoint CRUD หมวดหมู่ที่ขาดหายไปทั้งชุด (เดิมมีแค่ GET) และเชื่อมหน้า Admin Categories เข้าระบบ (เดิมสร้างไว้แต่ไม่มี route)
- แก้ password hash หลุดผ่าน API (`GET /users/:id` และตอนสร้าง user)
- แก้ draft/บทความที่ไม่เผยแพร่ ดูได้โดยไม่ต้องผ่านสิทธิ์ผ่าน `GET /articles/:id`
- แก้ editor เลี่ยงข้อจำกัดหมวดหมู่ที่ตัวเองมีสิทธิ์ได้ด้วยการส่ง `categoryId` เอง
- แก้ user role viewer (ควรอ่านอย่างเดียว) อัปโหลดไฟล์ได้ + ไม่จำกัดชนิดไฟล์อันตราย
- แก้ token เก่ายังใช้สิทธิ์เดิมได้แม้ admin เปลี่ยน role/ปิดบัญชีไปแล้ว (ตอนนี้เช็คสิทธิ์จาก DB สดทุก request)
- แก้ลบ Role/Category ที่ยังมีคนใช้งานอยู่ได้ (เสี่ยงข้อมูลเสียหาย)
- แก้ viewer/user ทั่วไปเข้าหน้าสร้าง/แก้ไขบทความได้ทาง URL ตรง

## [1.0.0]
เวอร์ชันตั้งต้นที่ export ออกมาจากระบบเดิม
